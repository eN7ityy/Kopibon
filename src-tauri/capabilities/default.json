{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "B1 baseline: core command/event plumbing only. B3 adds plugin permissions (opener, dialog, updater, notification) and the scoped asset protocol.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
