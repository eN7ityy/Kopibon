node_modules/
dist/
out/
.env
.claude
*.log

# Legacy Python-era config, read by nothing in src/. Kept locally for reference.
config.json

# Design docs and agent handoffs — working notes, not part of the application.
plans/
.eslintcache
.DS_Store
*.tsbuildinfo

# Keep old Python project files as reference, don't track in new project
.venv/
Logs/
oldScripts/
scripts/

# IDE
.idea/
*.swp
*.swo

# Port tooling under scripts/port is tracked (phase gates, corpus scripts);
# everything else in scripts/ stays untracked per the legacy rule above.
!scripts/
scripts/*
!scripts/port/
