//! Shared support for the headless CLI (`kopibon`): arg parsing helpers and
//! the harness ops the differential runner invokes
//! (docs/rust-port/08-subsystem-plans/01 §8). Ops are added by their WPs.

/// Parse a JSON file from stdin or a path — filled in by WP-A11's first slice.
pub fn harness_stub() {}
